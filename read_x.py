#!/Users/takashi/.pyenv/shims/python

def read_x(filepath):
    """
    
    """
    dim_strain = 0
    x = [[0 for j in range(dim_strain)] for i in range(dim_strain)]


def test_read_x():
    file1 = ""
    assert read_x()
